import XCTest
@testable import LGActivityIndicator

final class LGActivityIndicatorTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(LGActivityIndicator().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
